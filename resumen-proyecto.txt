
Proyecto: Automatización Inteligente de Reuniones con Coda.io
Fecha de documento: 2025-06-01

Objetivo general:
Desarrollar un sistema automatizado que procese las transcripciones de reuniones internas (vía Circleback), identifique temas clave, genere insights estructurados y los sincronice con tablas dinámicas en Coda.io. El sistema debe ser escalable, multiempresa, y capaz de reducir o eliminar el procesamiento de lenguaje natural (PLN) posterior gracias a la estructuración precisa desde Circleback.

---

⚙️ Alcance funcional del sistema

Entrada:
- Transcripción completa de la reunión.
- Metadatos (fecha, participantes, duración, etc.).

Procesamiento intermedio:
1. Detección de temas tratados.
2. Extracción de tareas, responsables y fechas.
3. Identificación de tareas anteriores mencionadas.
4. Clasificación por categorías: decisiones, ideas, tareas, bloqueos, riesgos, etc.

Salidas esperadas (en Coda.io):
- Creación de nuevas filas en tablas específicas.
- Actualización de tareas existentes si se mencionan nuevamente.
- Creación de nuevas tablas si se detecta un esquema nuevo.
- Histórico por reunión y proyecto.
- Seguimiento automático de tareas y decisiones.

---

🧠 Flujo lógico del sistema:

1. Webhook listener (API Gateway + Lambda/FastAPI).
2. Cola de mensajes (ej. SQS) para desacoplar procesamiento.
3. Backend en FastAPI que limpia y procesa la transcripción.
4. Clasificación y detección de entidades relevantes.
5. Lógica de decisión para crear, actualizar o sugerir nueva estructura.
6. Uso de API de Coda para insertar o actualizar registros.
7. Persistencia de logs y registros de auditoría.
8. Posibilidad de seguimiento entre reuniones vía reunión_id.

---

🏗️ Arquitectura técnica recomendada:

- Entrada: API Gateway + Lambda o FastAPI (verificación HMAC, control de IP).
- Mensajería: Amazon SQS (manejo de errores, reintentos).
- Procesamiento: FastAPI con spaCy + hashing + lógica de decisión.
- Embeddings opcionales: Sentence-Transformers + FAISS (para similitud).
- Almacenamiento: DynamoDB o RDS (solo si es necesario).
- Integración Coda: REST API con tokens OAuth o claves por workspace.
- Observabilidad: CloudWatch, Sentry, OpenTelemetry.
- Infraestructura: Docker, Terraform/CDK, deployment en AWS o Railway.

---

📁 Estructura sugerida en Coda.io:

1. Tabla: Tareas
   - tarea_id, tema_principal, descripcion_completa, responsable_directo, otros_participantes, fecha_limite, proyecto_o_area_relacionada, impacto_importancia, requiere_seguimiento, notas_adicionales

2. Tabla: PreguntasPendientes
   - pregunta_id, texto_pregunta, autor_pregunta, contexto, estado_actual, fecha_registro

3. Tabla: Feedback
   - feedback_id, autor_feedback, texto_feedback, categoría, urgencia, fecha_feedback

4. Tabla: RiesgosBloqueos
   - riesgo_id/bloqueo_id, tipo, descripción, origen, impacto_posible, acción_recomendada, responsable, fecha_identificación

5. Tabla: DecisionesEstratégicas
   - decision_id, descripción_decisión, justificación, participantes_clave, fecha_decisión, impacto_en, seguimiento_requerido, próxima_fecha_revisión

6. Tabla: IdeasSeguimientoInfo
   - idea_id / seguimiento_id / info_id, tema_principal, descripción, responsable_directo, otros_participantes, fecha_reunión, notas_adicionales

7. Tabla: ResumenReunion
   - reunion_id, fecha_reunión, titulo_reunión, resumen_general, participantes_principales, temas_clave, decisiones_tomadas, tareas_identificadas, bloqueos_detectados, seguimiento_requerido, proyectos_relacionados

8. Tabla: NuevoSchemaReunion
   - schema_id, motivo_genérico, campos_sugeridos (como lista de objetos), valor_ejemplo (como JSON)

---

🧩 Insights personalizados creados:

1. Tareas
2. Preguntas Pendientes
3. Feedback
4. RiesgosBloqueos
5. DecisionesEstratégicas
6. Ideas / Seguimiento / Información
7. ResumenReunion
8. NuevoSchemaReunion (genérico para casos no contemplados)

Cada uno incluye un ID determinista generado como hash SHA-1 truncado (8 caracteres) usando los campos principales de cada insight. Esto permite upserts automáticos en Coda.io.

---

🧠 Notas de diseño clave:
- Todo el contenido estructurado se genera directamente desde Circleback usando prompts en la sección “Description” de cada insight.
- Se evita el PLN adicional gracias al formato estructurado.
- Si Circleback devuelve un hash inválido, el backend puede regenerarlo usando SHA-1 real.
- El insight genérico permite extender el sistema a cualquier tipo de dato futuro no contemplado.

---

💡 Resultado final:
Un sistema robusto, escalable, y listo para convertirse en un SaaS que convierte conversaciones en acción estructurada.
